# Build output (production build lands in repo root)
/main.js

# Dependencies
node_modules/

# Local dev config (per-machine vault path)
.env
.env.local
.env.prod

# OS
.DS_Store

# End-to-end test artifacts (traces, screenshots, HTML reports)
e2e/artifacts/
