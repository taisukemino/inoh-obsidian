import type { InohSettings } from "../types";

export const DEFAULT_SETTINGS: InohSettings = {
  highlightEnabled: true,
  highlightStyle: "underline",
  tolerantMatching: false,
  selectedDeckId: null,
};
